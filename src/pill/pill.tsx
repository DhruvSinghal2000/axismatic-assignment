import * as React from 'react'; 

import * as styles from './pill.css'; 

export const Pill: React.FC = (): JSX.Element  => {
    return (
        <div className={styles.pill}>
            1 of 3
        </div>
    )
}